[project]
name = "devprofile"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
devprofile = "bin.devprofile:main"

[project.optional-dependencies]
dev = ["pytest>=8"]

[tool.pytest.ini_options]
testpaths = ["tests"]
