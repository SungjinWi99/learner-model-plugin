# 학습자 모델 코딩 에이전트 계층

코딩 에이전트 위에 얹혀, 학생의 개발 경험을 영속적으로 기억하고 그 기억에 따라
에이전트가 한 작업에서 얼마나/어떤 형태로 개입할지를 조절하는 계층.
부트캠프 코호트 단위로 배포된다.

## Language

**관측(Observation)**:
하나의 역량에 대한 학생의 한 차례 시도를, 시작부터 끝까지 일어난 그대로 기록한 것.
여러 턴에 걸칠 수 있으며 단위는 턴이 아니라 시도다. [[학습자 프로필]]에 남는 유일한 단위다.
_Avoid_: 경험, evidence, 이력, 로그

**원시 이벤트(Raw Event)**:
[[관측]]의 재료. 세션에서 실제로 일어난 일을 코드가 그대로 한 줄씩 적은 것이며 모델이 닿지 않는다.
학생의 행동만이 아니라 주입·자동 여유 전환 같은 시스템 자신의 행동도 여기 적힌다 —
그래서 관측이 아니다. 추출된 뒤 7일이 지나면 본문은 지워지고 흔적만 남는다.
_Avoid_: 로그, 트레이스, 히스토리

**에피소드(Episode)**:
하나의 관측이 포괄하는 시간 범위. 학생이 그 역량에 손을 대기 시작한 순간 열리고,
세션이 끝나거나 다른 역량으로 옮겨가거나 한동안 활동이 없으면 닫힌다.
_Avoid_: 세션, 턴, 대화

**해석(Judgment)**:
여러 관측을 근거로 도출한 역량 추정. 읽는 시점에 만들어지고 저장되지 않는다.
"혼자 해냈다", "이해가 부족하다" 같은 문장은 관측이 아니라 해석이다.
_Avoid_: 평가, 판정, 레벨링

**학습자 프로필(Learner Profile)**:
한 학생에 대해 누적된 관측의 집합. 코드나 개인정보를 담지 않는다.
_Avoid_: Student Model, 학생 기록, 성적표

**역량(Competency)**:
프로필이 관측을 걸어두는 **주소**. `langgraph/send`, `테스트`, `디버깅`처럼 셀 수 있는 항목이며,
코호트 커리큘럼에서 오는 뼈대와 커리큘럼에 직교하게 자라는 축으로 이루어진다.
학생이 그 주소에서 얼마나 하는지는 역량이 아니라 [[숙련도]]다.
_Avoid_: 스킬, 토픽, 카테고리

**개입(Intervention)**:
한 작업에서 에이전트가 취하는 태도. 강도의 크고 작음이 아니라,
그 작업의 인지 작업을 학생과 에이전트 사이에 어떻게 나누는지를 가리킨다.
_Avoid_: 개입 강도, 레벨, 모드

**제출(Submission)**:
학생이 자기 프로필을 열람하고, 지우고 싶은 부분을 지우고, 최종 확인한 뒤
플러그인 개발자에게 보내는 사본. 학생의 명시적 승인 없이는 발생하지 않는다.
_Avoid_: 리포팅, 동기화, 업로드

**코호트(Cohort)**:
하나의 커리큘럼을 공유하는 부트캠프 기수. 배포 단위이자 역량 뼈대의 출처.
_Avoid_: 클래스, 팀, 그룹

## Language — 개입 결정

**숙련도(Proficiency)**:
한 역량에서 학생이 눈앞의 작업을 도움 없이 끝낼 수 있는 **정도**. 관측에서 추론하며 저장하지 않는다.
역량이 *어디*라면 숙련도는 *얼마나*다. 두 말을 섞어 쓰지 않는다.
_Avoid_: 레벨, 등급, 실력, Competence(역량과 혼동된다)

**학습가치(Learning Value)**:
지금 이 작업을 학습 과제로 삼는 것의 가치. 숙련도가 양 끝일 때 낮고 그 사이에서 가장
높으며, 해당 역량이 학생의 목표와 얼마나 관련되는지에 비례한다.
_Avoid_: 난이도, 교육 효과

**여유(Slack)**:
학생이 지금 느려질 수 있는 정도. 시스템이 추론하지 않고 학생의 행동에서만 온다.
여유가 없으면 다른 모든 조건을 무시하고 개입이 침묵 대행으로 내려간다.
_Avoid_: 긴급도, 마감, 우선순위

**우회(Override)**:
제안된 개입을 학생이 한 번에 무르고 에이전트에게 그냥 시키는 행위.
여유를 알리는 유일한 입력이자, 개입 오판을 세는 유일한 계측 지점이다.
_Avoid_: 스킵, 취소, 끄기

**미룬 레슨(Deferred Lesson)**:
여유가 없어 침묵 대행으로 넘어갔지만 학습가치는 높았던 작업의 관측.
소모된 학습 기회가 아니라 나중에 되돌아올 수 있는 백로그로 남는다.
_Avoid_: 놓친 기회, 미완료 학습

### 개입 형태

**침묵 대행**: 에이전트가 전 단계를 맡고 설명하지 않는다.
**시범**: 에이전트가 전 단계를 맡되 그 과정을 설명한다. 분담은 침묵 대행과 같고 산출물이 다르다.
**골격**: 에이전트가 구조를 전부 쓰고 학생이 3~10줄의 빈칸을 채운다. 빈칸은 한 개념당 하나이며,
주석에는 무엇을 채워야 하는지와 참고 위치만 적고 답은 적지 않는다.
**설계→구현 분리**: 학생이 문제 정의와 설계를 맡고 에이전트가 구현한다.
**구현→검수 분리**: 학생이 구현까지 맡고 에이전트가 검토한다.

**개입 사유(Rationale)**:
선택한 개입 형태와 함께 학생에게 제시하는 한 줄 근거. 관측 하나만 인용한다.
학생이 근거를 보고 틀렸다고 말할 수 있게 하는 것이 목적이므로, 설득이 아니라 노출이다.
_Avoid_: 설명, 코칭 멘트, 피드백

## Language — 구성 요소

이름은 각각 `src/`의 파일 하나에 대응한다. 의존은 한 방향으로만 흐른다 —
**각 요소는 이 목록에서 자기보다 앞에 오는 것만 import한다.** 뒤의 것은 모른다.
새 요소를 넣을 때는 그 자리가 이 순서를 깨지 않는지부터 본다.

**호스트 어댑터(Host Adapter)** — `host_adapter.py`:
Claude Code와 Codex의 차이를 흡수하는 유일한 곳. 흡수하는 것은 셋이다.
쓰기 도구명(`Edit` `Write` `apply_patch`, 그리고 패치가 인자에 실려 오는 `Bash`)을 하나의
쓰기 사건으로 정규화하고, headless 호출 명령을 조립하고(`claude -p` / `codex exec`),
답변 첫 줄에서 개입 형태와 첫 세션 응답을 읽는다. 아무것도 import하지 않는다.
_Avoid_: 어댑터 레이어, 브리지

**프로필 저장소(Profile Store)** — `repository.py`:
`~/.devprofile/` 아래의 모든 읽기와 쓰기. **학습자 프로필에 닿는 유일한 요소다** —
다른 어떤 요소도 raw·관측·태그·프로필 파일을 직접 건드리지 않는다. 프로필 저장소를 거치지 않는
파일은 배포 설정(`config.json`)과 [[제출]] 사본뿐이고, 둘 다 프로필이 아니다.
경로는 고정이며 `DEVPROFILE_ROOT`로만 바꿀 수 있다(테스트용). 7일 폐기와 `extracted` 표시도
여기 있다. 아무것도 import하지 않는다.
_Avoid_: DAO, 스토리지 계층, DB

**이벤트 기록기(Event Recorder)** — `recorder.py`:
[[원시 이벤트]]를 한 줄씩 append한다. **판단하지 않고 모델을 부르지 않는다.**
프롬프트·주입·쓰기·턴 종료·자동 여유 전환이 각각 한 종류의 사건이다.
우회 감지도 여기서 하는데, 문구 매칭이지 추론이 아니다.
_Avoid_: 로거, 트래커, 콜렉터

**관측 추출기(Observation Extractor)** — `extractor.py`:
raw를 읽어 [[관측]]을 만든다. 모델을 부르는 두 곳 중 하나이며 `DEVPROFILE_EXTRACTOR_COMMAND`로
교체할 수 있다. 태그 목록에 **닫혀 있지 않아** 처음 보는 역량도 새 태그로 만들 수 있다.
`kind`가 행동 동사 아홉 개 안에 있는지 여기서 검증한다 — [[해석]]은 저장될 수 없다.
_Avoid_: 파서, 분석기, 요약기

**역량 태거(Competency Tagger)** — `tagger.py`:
지금 들어온 프롬프트가 **어느 [[역량]]에 관한 작업인지** 태그 이름 목록을 낸다.
모델을 부르는 두 곳 중 나머지 하나이며 `DEVPROFILE_TAGGER_COMMAND`로 교체할 수 있다.
관측 추출기와 달리 태그 목록에 **닫혀 있다** — `tags.json`에 없는 이름은 낼 수 없다.
`UserPromptSubmit` 안에서 돌기 때문에 10초 예산을 두고, 실패하면 키워드 매칭으로 떨어진다.
_Avoid_: 분류기, 라우터

**관측 검색기(Observation Retriever)** — `retriever.py`:
태그로 거르고, 최신순으로 세우고, 상위 8개를 취한다. 그게 전부다.
점수 함수도 감쇠 상수도 반감기도 없다.
_Avoid_: 랭커, 쿼리 엔진, 스코어러

**개입 결정기(Intervention Decider)** — `decider.py`:
순수 함수. 검색 결과를 받아 [[개입]] 형태와 [[개입 사유]]를, 그리고 **왜 그 형태인지**를
함께 낸다. 사유 값은 다섯이다: `trivial` `no_tags` `no_slack` `proficiency_ceiling`
`intervention`. 같은 침묵 대행이라도 어느 게이트에서 나왔는지 구분되며, [[미룬 레슨]]은
`no_slack`에서만 생긴다. [[숙련도]] 계산도 여기 있고 모델을 부르지 않는다.
_Avoid_: 정책 엔진, 룰 엔진, 플래너

**렌더러(Renderer)** — `renderer.py`:
사람이나 에이전트가 읽는 텍스트를 만드는 유일한 곳. 주입 블록, 프로필 화면,
첫 세션 질문 셋이다. 주입은 관측당 잘라내고 전체도 잘라, 프로필이 커져도 일정하게 유지한다.
_Avoid_: 포매터, 뷰, 템플릿

**관측 파이프라인(Observation Pipeline)** — `observation_pipeline.py`:
관측 추출기가 낸 후보를 기존 관측에 upsert하고 [[에피소드]]를 닫는다.
새 태그를 보류로 만들고 2회째에 승격시키는 것도 여기다. 관측 추출기와 프로필 저장소 사이에 있다.
_Avoid_: 매니저, 서비스, 오케스트레이터

**프로필 제출기(Profile Submitter)** — `submission.py`:
[[제출]] 사본을 만들고, 학생이 명시적으로 승인했을 때만 클립보드에 복사하고 폼을 연다.
폼 URL은 `config.json`이 정본이고 `DEVPROFILE_FORM_URL`이 덮어쓴다. 설정되지 않았으면
전송 직전에 눈에 띄게 실패하지만, 열람과 편집은 그와 무관하게 동작한다.
